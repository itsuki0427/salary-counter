import React from 'react';
import { SalaryCounterComponent } from './SalaryCounterComponent';

export const App: React.FC = () => {
  return (
    <div>
      <SalaryCounterComponent />
    </div>
  );
};

export default App;
